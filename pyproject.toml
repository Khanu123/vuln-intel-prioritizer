[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vuln-intel-prioritizer"
version = "1.0.0"
description = "Risk-based vulnerability prioritization demo."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
